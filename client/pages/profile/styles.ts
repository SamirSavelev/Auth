import styled from "styled-components";

const Bold = styled.span`
  font-weight: 70;
`;

const Container = styled.div`
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 50px;
`;

export const ProfileStyles = { Bold, Container };
